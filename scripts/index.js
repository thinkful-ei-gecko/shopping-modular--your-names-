'use strict';
/* global shoppingList, cuid */

// eslint-disable-next-line no-unused-vars

function main() {
  shoppingList.bindEventListeners();
  shoppingList.render();
}

$(main);

console.log(Item);
console.log(foo);